const defaultState = {
    fetching: false,
    error: null,
    user: null,
    token: null
};

export default function reducer(state = defaultState, action) {
    switch (action.type) {
        // @TODO
    }

    return state;
}
